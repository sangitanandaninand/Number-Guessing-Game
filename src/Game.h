#pragma once

class Game {
private:
}
int secretNumber; //Holds the random number to guess
int tries; //Counts how much guesses the user has made which keeps track of the guesses.

public:
Game(); //Constructor
void startGame(); {

};